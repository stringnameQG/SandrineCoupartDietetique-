{% extends 'base.html.twig' %}

{% block title %}Contacte{% endblock %}

{% block body %}
    <h1>Contacte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contacte.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contacte.email }}</td>
            </tr>
            <tr>
                <th>Demande</th>
                <td>{{ contacte.demande }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contacte_index') }}">back to list</a>

    <a href="{{ path('app_contacte_edit', {'id': contacte.id}) }}">edit</a>

    {{ include('contacte/_delete_form.html.twig') }}
{% endblock %}
